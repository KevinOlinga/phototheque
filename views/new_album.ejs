<!DOCTYPE html>
<html lang="en">
  <%- include('./layouts/_header', {cssFile: cssFile}) %>
  <body>
    <h1 class="sub-heading">Creer un album, c'est construire une vie...</h1>
    <div class="form-container">
      <h1>Créer un nouvel album</h1>
      <form action="/albums/new" method="POST">
        <div class="form-group">
          <label for="title">Titre de l'album:</label>
          <input type="text" id="title" name="title" required />
        </div>
        <div class="centered">
          <button type="submit" class="btn btn-green">Créer l'album</button>
        </div>
      </form>
      <div class="centered">
        <a href="/albums" class="btn btn-yellow">Retour aux albums</a>
      </div>
    </div>
    <style>
      .banner {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        font-size: 3em;
        font-family: 'Chaparral Pro', serif;
        font-style: italic;
        background: -webkit-linear-gradient(#ffd700, #d4af37);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        padding: 20px;
        text-align: center;
        z-index: 1000;
      }
    </style>
  </body>
</html>
